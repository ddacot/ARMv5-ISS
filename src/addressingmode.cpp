#include "addressingmode.h"

AddressingMode::AddressingMode()
{
    //based on the instruction's addresing mode, point Source and Destination to the appropriate memories (reg/mem
    //or immediate value
}
